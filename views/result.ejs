<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Weather Result</title>
  <link rel="stylesheet" href="/styles/main.css"/>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"/>
</head>
<body class="bg-light text-center p-5">
  <div class="container">
    <h1 class="mb-4">🌤️ Weather Forecast for <%= city %></h1>

    <% if (summaries.length === 0) { %>
        <p>No forecast found for the selected time range.</p>
      <% } else { %>
        <% summaries.forEach(item => { %>
          <div class="card mb-3 shadow-sm">
            <div class="card-body">
              <h5 class="card-title"><%= new Date("1970-01-01T" + startTime + ":00").toLocaleTimeString("en-PH", {
                hour: "numeric", hour12: true 
              }) %> to 
              <%= new Date("1970-01-01T" + endTime + ":00").toLocaleTimeString("en-PH", {
                hour: "numeric", hour12: true 
              }) %></h5>
              <p>
                <strong><%= item.desc %></strong><br/>
                🌡️ <%= item.tempC %>°C / <%= item.tempF %>°F<br/>
                <span class="text-info fw-bold"><%= item.suggestion %></span>
              </p>
            </div>
          </div>
        <% }); %>
      <% } %>

    <a href="/" class="btn btn-secondary mt-4">Check Another City</a>
  </div>
</body>
</html>
